const Submitted = () => {
  return (
    <section>
      <h1>Thank you for your message.</h1>;
      <p>
        If this form was completed your message would have been sent to me.
        Sadly it's not, so it wasn't
      </p>
    </section>
  );
};

export default Submitted;
